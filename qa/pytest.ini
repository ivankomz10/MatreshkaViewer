[pytest]
# Quiet by default and loud when something fails: these tests take a while,
# and the useful output is the one line saying what the window did instead.
addopts = -q -rxX --tb=short
testpaths = .
python_files = test_*.py
